param int A = 1;
param int B = A * 2;

inline fn g<A,B>(stack u64[A] a, reg ptr u64[B] b) -> reg u64 {
    reg u64 r;
    r = a[1];
    return r; 
}//<>

export fn f() -> reg u64 {
    stack u64[10] a;
    stack u64[10] b;
    reg u64 x y;
    x = g<10,10>(a, b);
    y = g<9+A, B*5>(a, b);
    x += y;
    return x;
} 
